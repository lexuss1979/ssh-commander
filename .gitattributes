# Нормализация текстовых файлов на уровне атрибутов, а не core.autocrlf
# хоста: в репозитории хранится LF, дифф одинаков при любой локальной
# конфигурации (без этого файла тот же git diff под autocrlf=false
# показывает фантомную перезапису целых файлов из-за CRLF).
* text=auto
