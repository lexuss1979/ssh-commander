node_modules/
dist/
.env
# Данные установки: профили с SSH-паролями, подключения БД, диалоги и память
# агента, отпечатки ключей хостов, журнал расходов, а также tmp- и
# *.corrupt-*-копии сторов. Перечислять файлы по одному нельзя: новый стор
# (known-hosts.json) и бэкапы corrupt-guard мимо такого списка проходили.
data/*
!data/.gitkeep
keys/*
!keys/.gitkeep
*.log
.DS_Store
.qwen/
.worktrees/
.eslintcache
# Локальные материалы продвижения — не публикуются
promo/
# HTML-превью UI-прототипов (локальные моки, не часть приложения)
previews/
